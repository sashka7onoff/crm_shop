services:
  web:
    build: .
    env_file:
      - .env
    volumes:
      - static_volume:/app/staticfiles

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web

volumes:
  static_volume:
